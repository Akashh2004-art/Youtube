import React from "react";
import "./sideNavbar.css"; // Ensure the file name matches exactly

const SideNavbar = () => {
    return (
        <div>SideNavbar</div>
    );
};

export default SideNavbar;
